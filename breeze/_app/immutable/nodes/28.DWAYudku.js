import{E as e,N as t,T as n,U as r,_ as i,c as a,d as o,h as s,j as c,k as l,o as u}from"../chunks/BgsmUUjA.js";import"../chunks/xihTtKlq.js";import{n as d,t as f}from"../chunks/DGZ0BS5H.js";var p=`Video Streaming — Breeze Docs`,m=`Byte-range video streaming for Breeze with seek support, signed URLs, path traversal protection, and live dashboard stats.`,h=d+`/docs/video/`,g=i(`<meta name="description"/> <link rel="canonical"/> <meta property="og:type" content="article"/> <meta property="og:site_name"/> <meta property="og:title"/> <meta property="og:description"/> <meta property="og:url"/> <meta name="twitter:card" content="summary"/> <meta name="twitter:title"/> <meta name="twitter:description"/>`,1),_=i(`<h1 id="video-streaming">Video streaming<a class="heading-anchor" aria-hidden="true" tabindex="-1" href="#video-streaming">#</a></h1> <p>Byte-range video streaming for Breeze — serves a directory of media files so
that browsers can <strong>seek</strong>, which is the whole difficulty and the reason a
plain static file handler isn't enough.</p> <!> <p>That registers <code>GET</code> and <code>HEAD</code> on <code>/videos/*filepath</code>.</p> <!> <h2 id="why-a-static-handler-isnt-enough">Why a static handler isn't enough<a class="heading-anchor" aria-hidden="true" tabindex="-1" href="#why-a-static-handler-isnt-enough">#</a></h2> <p>A <code>&lt;video&gt;</code> element never downloads a file in one go. It reads the
container header, jumps to wherever the viewer clicked, and reads forward
from there. Each jump is a separate request carrying a <code>Range</code> header, and
the server has to reply with <strong>206 Partial Content</strong>, exactly the bytes
asked for, and a <code>Content-Range</code> that describes them.</p> <p>Ignore <code>Range</code> and the video still plays — which is what makes the bug
expensive to find. The scrubber is simply dead: the browser can't ask for
the middle of a file it's being handed sequentially.</p> <p>There's a structural reason this couldn't be a thin wrapper, too. Breeze's <code>HTTPResponse.Bytes()</code> always emits <code>Content-Length: len(Body)</code> and always
writes <code>Body</code>, since it's built to describe a complete in-memory response.
A <code>206</code> whose length is one slice of a file, a <code>304</code> with no body, and a
multi-write stream whose head goes out before the bytes exist are all
outside what it can express — so this package sets <code>ctx.Res = nil</code>, takes
over the connection, and serialises its own head.</p> <h2 id="memory">Memory<a class="heading-anchor" aria-hidden="true" tabindex="-1" href="#memory">#</a></h2> <p>One chunk in flight per response — 256 KiB by default, from a pool,
returned when the write completes. Ten thousand viewers cost ten thousand
chunks, not ten thousand copies of the file.</p> <p>Players routinely open with <code>Range: bytes=0-</code>, meaning "the rest of the
file". Answering literally would pin an entire movie in memory and defeat
seeking, so open-ended requests are capped at <code>MaxChunkSize</code> (4 MiB
default) — a server is explicitly allowed to return less than was asked
for, provided <code>Content-Range</code> says what it actually sent.</p> <p>Measured on an i5-11400F:</p> <!> <p>Allocations per request do not grow with file size.</p> <h2 id="security">Security<a class="heading-anchor" aria-hidden="true" tabindex="-1" href="#security">#</a></h2> <p>The request path is treated as hostile, and the <strong>order</strong> of the checks is
the security property:</p> <ol><li><strong>Percent-decode first</strong> — otherwise <code>%2e%2e%2f</code> walks straight past a
check that only understands literal dots.</li> <li><strong>Reject NUL and backslash</strong> — <code>safe.mp4\\x00../../etc/passwd</code> passes a
naive suffix check and opens something else; <code>..\\..\\x</code> traverses on
Windows but survives a slash-only cleaner.</li> <li><strong>Reject any <code>..</code> segment outright.</strong> Cleaning it away would be <em>safe</em> — <code>path.Clean("/"+"../x")</code> gives <code>/x</code>, inside the root — but it silently
rewrites an attack into a legitimate-looking lookup, so neither the logs
nor the <code>Authorize</code> callback ever learn an attack was attempted. No real
client puts <code>..</code> in a media URL, so failing closed costs nothing.</li> <li><strong>Hide dotfiles</strong> by default, so a stray <code>.env</code> in the media root is
unreachable.</li> <li><strong>Extension allow-list</strong>, so a new dangerous type cannot silently become
servable.</li> <li><strong>Only then touch the disk</strong>, and prove containment against the
symlink-resolved real path — catching the subtle case where every
segment is innocent but one is a link out of the tree.</li></ol> <p>Everything about a file's existence returns <strong>404, never 403</strong>, so the
filesystem can't be mapped by watching which refusals differ. The real
reason goes to <code>OnError</code> and the collector, never to the wire. Header
values are stripped of CR/LF at the single point where bytes are
serialised, so a filename cannot inject a second response.</p> <h3 id="signed-urls">Signed URLs<a class="heading-anchor" aria-hidden="true" tabindex="-1" href="#signed-urls">#</a></h3> <p>Verified <strong>before</strong> any filesystem access, so an unauthenticated flood
costs no disk I/O:</p> <!> <p>Comparison is constant-time, and the expiry is inside the signed payload so
it can't be extended by editing the query string.</p> <h2 id="caching">Caching<a class="heading-anchor" aria-hidden="true" tabindex="-1" href="#caching">#</a></h2> <p><code>ETag</code> (size + mtime) and <code>Last-Modified</code> on every success. Conditional
requests are answered <strong>before the file is opened</strong>, so a revalidation
costs a stat instead of a transfer. <code>If-None-Match</code> takes precedence over <code>If-Modified-Since</code>, since a date is only second-accurate and would keep
serving a stale body for up to a second after an edit.</p> <p><code>If-Range</code> is honoured, which is what makes a resumed download safe: if the
file changed while the client was away, the whole file is sent rather than
a slice that would corrupt the client's copy.</p> <h2 id="observability">Observability<a class="heading-anchor" aria-hidden="true" tabindex="-1" href="#observability">#</a></h2> <p>Every request publishes an <code>observability.Signal</code> and a <code>StreamServed</code> event:</p> <!> <p>A viewer who closes the tab mid-stream is reported as <strong>cancelled, not
failed</strong>. In video that's the most common way a request ends — every seek
and every closed tab aborts a transfer in flight — and counting those as
errors would make a healthy server look like an outage. <code>Bytes</code> reports
what actually left, so a transfer abandoned at 90% is distinguishable from
one that never started.</p> <h3 id="the-dashboards-video-tab">The dashboard's Video tab<a class="heading-anchor" aria-hidden="true" tabindex="-1" href="#the-dashboards-video-tab">#</a></h3> <!> <p>A separate call from <code>AttachEvents</code> on purpose, so an application with no
media pays nothing — the tracker is only allocated when you attach it.</p> <p>It isn't just the Live Requests feed because streaming breaks the
one-row-per-request model. A single viewer emits hundreds of range requests
for one file, so that feed would show the same filename repeatedly,
interleaved with unrelated traffic — and it can't report throughput at all,
since bandwidth is a property of a <em>stream</em>, not of any single request. The
Video tab aggregates <strong>by file</strong>: bytes in flight, MB/s, seeks,
disconnects, and errors per title.</p> <p>Rates are computed over a fixed 10-second window rather than the span
between samples, because two requests 3 ms apart would otherwise read as
tens of MB/s of "sustained" throughput. <code>304</code>s are excluded from that
window, so a well-cached file doesn't appear slow. The file table is
bounded, and idle files are evicted before active ones, so a client probing
random paths can't push a live stream off the page. Finished requests
remain in the observability ring buffer either way — the tab is a live
view, not the historical record.</p> <h2 id="configuration">Configuration<a class="heading-anchor" aria-hidden="true" tabindex="-1" href="#configuration">#</a></h2> <table><thead><tr><th>Field</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>Root</code></td><td>—</td><td>required; resolved absolutely, symlinks evaluated once at mount</td></tr><tr><td><code>Prefix</code></td><td><code>/videos</code></td><td>route is <code>Prefix + "/*filepath"</code></td></tr><tr><td><code>Extensions</code></td><td>common video + HLS/DASH</td><td>allow-list, case-insensitive</td></tr><tr><td><code>AllowHidden</code></td><td><code>false</code></td><td>dotfiles stay unreachable</td></tr><tr><td><code>FollowSymlinks</code></td><td><code>false</code></td><td>target must still resolve inside <code>Root</code></td></tr><tr><td><code>ChunkSize</code></td><td>256 KiB</td><td>bytes per write</td></tr><tr><td><code>MaxChunkSize</code></td><td>4 MiB</td><td>cap for open-ended ranges; negative disables</td></tr><tr><td><code>CacheControl</code></td><td><code>public, max-age=86400</code></td><td><code>"-"</code> omits the header</td></tr><tr><td><code>AllowedOrigins</code></td><td>none</td><td><code>"*"</code> allows any; <code>Vary: Origin</code> always sent</td></tr><tr><td><code>Authorize</code></td><td>nil</td><td>runs on the clean name, before any disk access</td></tr><tr><td><code>Secret</code></td><td>nil</td><td>enables signed URLs</td></tr><tr><td><code>OnError</code></td><td>nil</td><td>receives the internal error, never sent to clients</td></tr></tbody></table> <h2 id="status-codes">Status codes<a class="heading-anchor" aria-hidden="true" tabindex="-1" href="#status-codes">#</a></h2> <table><thead><tr><th>Code</th><th>When</th></tr></thead><tbody><tr><td>200</td><td>only an empty file, with <code>Content-Length: 0</code></td></tr><tr><td>206</td><td>every other success — a valid <code>Range</code>, and also a request with <strong>no</strong> <code>Range</code> or a malformed one (see below)</td></tr><tr><td>304</td><td><code>If-None-Match</code> / <code>If-Modified-Since</code> hit</td></tr><tr><td>403</td><td>signature missing, invalid or expired; <code>Authorize</code> refused</td></tr><tr><td>404</td><td>missing, traversal, hidden, wrong type, directory, symlink</td></tr><tr><td>416</td><td>well-formed but unsatisfiable range; carries <code>Content-Range: bytes */size</code></td></tr></tbody></table> <h3 id="a-request-with-no-range-still-gets-206">A request with no <code>Range</code> still gets 206<a class="heading-anchor" aria-hidden="true" tabindex="-1" href="#a-request-with-no-range-still-gets-206">#</a></h3> <p>RFC 9110 permits serving the whole representation when the client sends no <code>Range</code> — what a static file server does. This mount answers with the first <code>ChunkSize</code> bytes instead, because the alternative for video is streaming
an entire movie through one pooled buffer while the viewer waits and can't
seek. A malformed <code>Range</code> — which RFC 9110 requires be ignored — takes the
same path, so it behaves as if no <code>Range</code> had been sent. <code>Content-Range</code> reports the full size in both cases, so a player learns the duration and
continues with explicit ranges.</p> <div class="callout warn">The consequence is worth knowing: a client that does not understand <code>Content-Range</code> receives a truncated file and no error — <code>curl -O</code> on a 100 MB video saves 256 KB and exits 0. Anything
that fetches these URLs as plain downloads (a backup script, a CDN origin
pull that ignores partial responses, a scraper) needs to send an explicit <code>Range: bytes=0-</code> and follow <code>Content-Range</code>, or read
the file from disk rather than through the mount. Players and browsers are
unaffected — they range-request by nature.</div>`,1);function v(i){var d=_();a(`izasz0`,r=>{var i=g(),a=c(i);u(a,`content`,m);var o=t(a,2),d=t(o,4),_=t(d,2);u(_,`content`,p);var v=t(_,2);u(v,`content`,m);var y=t(v,2),b=t(y,4);u(b,`content`,p);var x=t(b,2);u(x,`content`,m),e(()=>{u(o,`href`,h),u(d,`content`,f),u(y,`content`,h)}),n(()=>{l.title=`Video Streaming — Breeze Docs`}),s(r,i)});var v=t(c(d),4);o(v,()=>`<pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117" tabindex="0"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">router </span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">:=</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3"> breeze.</span><span style="--shiki-light:#8250DF;--shiki-dark:#D2A8FF">NewRouter</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">()</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">if</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3"> err </span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">:=</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3"> video.</span><span style="--shiki-light:#8250DF;--shiki-dark:#D2A8FF">Mount</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">(router, </span><span style="--shiki-light:#953800;--shiki-dark:#FFA657">video</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">.</span><span style="--shiki-light:#953800;--shiki-dark:#FFA657">Config</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">{Root: </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF">"./media"</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">}); err </span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">!=</span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF"> nil</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3"> {</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">	log.</span><span style="--shiki-light:#8250DF;--shiki-dark:#D2A8FF">Fatal</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">(err)</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">}</span></span></code></pre>`);var y=t(v,4);o(y,()=>`<pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117" tabindex="0"><code><span class="line"><span style="--shiki-light:#953800;--shiki-dark:#FFA657">go</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF"> run</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF"> ./cmd/video-example</span></span></code></pre>`);var b=t(y,18);o(b,()=>`<pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117" tabindex="0"><code><span class="line"><span>BenchmarkStreamChunk-12     691416 ns/op   6066 MB/s   1655 B/op    4 allocs/op</span></span>
<span class="line"><span>BenchmarkStreamSeek-12       92186 ns/op   2844 MB/s   1655 B/op    4 allocs/op</span></span>
<span class="line"><span>BenchmarkNormalize-12          342 ns/op                128 B/op    3 allocs/op</span></span>
<span class="line"><span>BenchmarkParseRange-12          88 ns/op                 16 B/op    1 allocs/op</span></span></code></pre>`);var x=t(b,16);o(x,()=>`<pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117" tabindex="0"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">video.</span><span style="--shiki-light:#8250DF;--shiki-dark:#D2A8FF">Mount</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">(router, </span><span style="--shiki-light:#953800;--shiki-dark:#FFA657">video</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">.</span><span style="--shiki-light:#953800;--shiki-dark:#FFA657">Config</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">{</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">	Root:   </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF">"./media"</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">,</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">	Secret: []</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">byte</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">(os.</span><span style="--shiki-light:#8250DF;--shiki-dark:#D2A8FF">Getenv</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">(</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF">"VIDEO_SECRET"</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">)),</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">})</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">url </span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">:=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF"> "/videos/movie.mp4?"</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72"> +</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3"> video.</span><span style="--shiki-light:#8250DF;--shiki-dark:#D2A8FF">Sign</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">(secret, </span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF">"movie.mp4"</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">, </span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF">10</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">*</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">time.Minute)</span></span></code></pre>`);var S=t(x,14);o(S,()=>`<pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117" tabindex="0"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">events.</span><span style="--shiki-light:#8250DF;--shiki-dark:#D2A8FF">OnType</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">(</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">func</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">(</span><span style="--shiki-light:#953800;--shiki-dark:#FFA657">_</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72"> *</span><span style="--shiki-light:#953800;--shiki-dark:#FFA657">events</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">.</span><span style="--shiki-light:#953800;--shiki-dark:#FFA657">Context</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">, </span><span style="--shiki-light:#953800;--shiki-dark:#FFA657">e</span><span style="--shiki-light:#953800;--shiki-dark:#FFA657"> video</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">.</span><span style="--shiki-light:#953800;--shiki-dark:#FFA657">StreamServed</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">) </span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">error</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3"> {</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">	log.</span><span style="--shiki-light:#8250DF;--shiki-dark:#D2A8FF">Printf</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">(</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF">"</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">%s</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF">: </span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">%d</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF"> bytes in </span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">%v</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF">"</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">, e.File, e.Bytes, e.Duration)</span></span>
<span class="line"><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">	return</span><span style="--shiki-light:#0550AE;--shiki-dark:#79C0FF"> nil</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">})</span></span></code></pre>`);var C=t(S,6);o(C,()=>`<pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117" tabindex="0"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">coll </span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">:=</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3"> dashboard.</span><span style="--shiki-light:#8250DF;--shiki-dark:#D2A8FF">Install</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">(app, router, </span><span style="--shiki-light:#953800;--shiki-dark:#FFA657">dashboard</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">.</span><span style="--shiki-light:#953800;--shiki-dark:#FFA657">Config</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">{})</span></span>
<span class="line"><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72">defer</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3"> coll.</span><span style="--shiki-light:#8250DF;--shiki-dark:#D2A8FF">AttachVideo</span><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3">(events.Default)()</span></span></code></pre>`),r(20),s(i,d)}export{v as component};